# 🏗️ Arquitectura del Sistema - Vista Completa

## 📐 Diagrama de Capas

```
┌─────────────────────────────────────────────────────────────┐
│                    CAPA DE PRESENTACIÓN                      │
│  ┌──────────────┐              ┌──────────────────────────┐ │
│  │     CLI      │              │    MainFrame (Swing)     │ │
│  │  (consola)   │              │   (interfaz gráfica)     │ │
│  └──────┬───────┘              └────────────┬─────────────┘ │
│         │                                   │               │
│         │ implements Observer               │               │
│         └───────────────┬───────────────────┘               │
└─────────────────────────┼─────────────────────────────────┘
                          │
                    update() cuando hay cambios
                          │
┌─────────────────────────┼───────────────────────────────────┐
│               CAPA DE APLICACIÓN (Controller)                │
│                         │                                    │
│         ┌───────────────▼─────────────────┐                 │
│         │        TomaService              │                 │
│         │  (Subject + Scheduler)          │                 │
│         │                                 │                 │
│         │  - ScheduledExecutorService     │                 │
│         │  - verificarRecordatorios()     │                 │
│         │  - emitirNotificacion()         │                 │
│         │  - pasarAAplazado()             │                 │
│         │  - pasarAPerdido()              │                 │
│         │  - notifyObservers()            │                 │
│         └────────┬────────────────────────┘                 │
│                  │ usa DAOs                                 │
└──────────────────┼──────────────────────────────────────────┘
                   │
┌──────────────────┼──────────────────────────────────────────┐
│                  │    CAPA DE DOMINIO                        │
│  ┌───────────────▼────────────────────────────────────────┐ │
│  │  Paciente | Medicamento | Recordatorio | Notificacion  │ │
│  │  Cuidador | PacienteMedicamento | Adherencia | Rutina  │ │
│  └─────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
                   │
┌──────────────────┼──────────────────────────────────────────┐
│         CAPA DE INFRAESTRUCTURA (Persistencia)               │
│                  │                                           │
│  ┌───────────────▼─────────────────────────────────────┐    │
│  │                  DAOs (interfaces)                   │    │
│  │  RecordatorioDAO | NotificacionDAO | AdherenciaDAO  │    │
│  │  PacienteDAO | MedicamentoDAO | CuidadorDAO         │    │
│  └───────────────┬─────────────────────────────────────┘    │
│                  │                                           │
│  ┌───────────────▼─────────────────────────────────────┐    │
│  │            DAOImpl (implementaciones)               │    │
│  │  RecordatorioDAOImpl | NotificacionDAOImpl | etc.   │    │
│  └───────────────┬─────────────────────────────────────┘    │
│                  │ JDBC                                      │
│  ┌───────────────▼─────────────────────────────────────┐    │
│  │              ConexionDB (H2)                        │    │
│  │  - getConnection()                                  │    │
│  │  - initSchemaIfAbsent()                             │    │
│  └─────────────────────────────────────────────────────┘    │
└───────────────────────────────────────────────────────────────┘
                   │
┌──────────────────┼──────────────────────────────────────────┐
│                  │     BASE DE DATOS                         │
│  ┌───────────────▼─────────────────────────────────────┐    │
│  │             H2 Database (data/db.mv.db)             │    │
│  │                                                      │    │
│  │  Tables: paciente, medicamento, recordatorio,       │    │
│  │          notificacion, adherencia, cuidador, etc.   │    │
│  └─────────────────────────────────────────────────────┘    │
└───────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo del Scheduler (TomaService)

### Flujo completo desde inicio hasta notificación:

```
┌─────────────────────────────────────────────────────────────┐
│ 1. INICIO DE LA APLICACIÓN                                  │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
                    Main.main()
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
  initSchema()    tomaService.start()   CLI.iniciar()
        │                 │                 │
        │                 │                 │
        │         ┌───────▼────────┐        │
        │         │ ScheduledExecutor│       │
        │         │ scheduleAtFixedRate()   │
        │         │ (cada 60 seg)   │       │
        │         └───────┬────────┘        │
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
┌─────────────────────────▼─────────────────────────────────┐
│ 2. VERIFICACIÓN PERIÓDICA (cada 60 segundos)              │
└────────────────────────────────────────────────────────────┘
                          │
            verificarRecordatorios()
                          │
        ┌─────────────────┴─────────────────┐
        │                                   │
        ▼                                   ▼
procesarPendientes()             procesarAplazados()
        │                                   │
        │                                   │
   ┌────┴─────┐                       ┌────┴─────┐
   │          │                       │          │
   ▼          ▼                       ▼          ▼
Notificar  Aplazar              ¿Perdido?     [nada]
   │          │                       │
   │          │                       │
   └──────────┴───────────────────────┘
              │
              ▼
    registrarAdherencia()
              │
              ▼
      notifyObservers()
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. ACTUALIZACIÓN DE UI (Observer Pattern)                   │
└─────────────────────────────────────────────────────────────┘
              │
      ┌───────┴────────┐
      │                │
      ▼                ▼
  CLI.update()   MainFrame.update()
      │                │
      │                │
  Actualizar      Actualizar
  consola         interfaz gráfica
```

---

## 🎯 Flujo de Estados de un Recordatorio

```
                     CREACIÓN
                        │
                        ▼
              ┌─────────────────┐
              │   PENDIENTE     │
              └────────┬────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
   [Hora - 5min]  [Ventana OK]  [Ventana expiró]
        │              │              │
        ▼              │              ▼
  ┌──────────┐         │       ┌──────────┐
  │NOTIFICADO│         │       │ APLAZADO │
  └──────────┘         │       └─────┬────┘
                       │             │
                       │      ┌──────┴─────────┐
                       │      │                │
                       │      ▼                ▼
                       │  [Usuario   [Tiempo recuperación
                       │   marca]     superado]
                       │      │                │
                       ▼      ▼                ▼
                  ┌───────────┐         ┌──────────┐
                  │   HECHO   │         │ PERDIDO  │
                  └───────────┘         └──────────┘
                       │                      │
                       └──────────┬───────────┘
                                  ▼
                         Registro en ADHERENCIA
                                  │
                                  ▼
                         Generar próximo recordatorio
                         (si es medicación recurrente)
```

---

## 📊 Modelo de Datos (Relaciones clave)

```
┌──────────────┐
│  Cuidador    │
│  - id        │
│  - nombre    │
└──────┬───────┘
       │ 1:N
       │
       ▼
┌──────────────┐
│  Paciente    │
│  - id        │
│  - nombre    │
│  - cuidador_id│
└──────┬───────┘
       │ 1:N
       │
       ├────────────────────────────┐
       │                            │
       ▼ N:M                        ▼ 1:N
┌──────────────┐            ┌──────────────┐
│ Medicamento  │            │ Recordatorio │
│  - id        │            │  - id        │
│  - nombre    │            │  - paciente_id│
└──────┬───────┘            │  - tipo      │
       │                    │  - estado    │
       │ N:M                │  - programado│
       │                    └──────┬───────┘
       ▼                           │
┌──────────────────┐               │ 1:N
│PacienteMedicamento│              │
│  - paciente_id   │◄──────────────┤
│  - medicamento_id│               │
│  - dosis         │               ├────────────┐
│  - intervalo     │               │            │
│  - proxima_toma  │               ▼ 1:N        ▼ 1:N
└──────────────────┘        ┌────────────┐ ┌───────────┐
                            │Notificacion│ │Adherencia │
                            │ - id       │ │ - id      │
                            │ - emitida  │ │ - accion  │
                            │ - canal    │ │ - timestamp│
                            └────────────┘ └───────────┘
```

---

## 🔐 Patrones de Diseño Implementados

### 1. **Observer Pattern**
```
Subject: TomaService
   │
   ├─ Observer: CLI
   └─ Observer: MainFrame (futuro)

Uso: Notificar cambios en recordatorios a la UI
```

### 2. **DAO Pattern**
```
Interface: RecordatorioDAO
   │
   └─ Implementation: RecordatorioDAOImpl
      
Beneficio: Abstracción de persistencia
```

### 3. **MVC (Model-View-Controller)**
```
Model: domain/* (Paciente, Medicamento, etc.)
View: ui/* (CLI, MainFrame)
Controller: controller/* (TomaService)
```

### 4. **Singleton (implícito en ConexionDB)**
```
ConexionDB.getConnection()
   └─ Siempre retorna la misma conexión
```

---

## ⚡ Decisiones Arquitectónicas Clave

### ¿Por qué ScheduledExecutorService?
- ✅ Thread-safe
- ✅ Manejo robusto de excepciones
- ✅ API moderna y profesional
- ❌ NO usar Timer (deprecated pattern)

### ¿Por qué H2 embebido?
- ✅ No requiere instalación externa
- ✅ Perfecto para prototipo/demo
- ✅ Compatible con SQL estándar
- 🔄 Fácil migrar a PostgreSQL/MySQL después

### ¿Por qué Observer?
- ✅ Desacoplamiento UI ↔ Backend
- ✅ Múltiples UIs pueden coexistir
- ✅ Testing más fácil
- ✅ Escalable a eventos complejos

### ¿Por qué verificar cada 60 segundos?
- ✅ Balance entre precisión y eficiencia
- ✅ Medicaciones no requieren precisión de segundos
- ✅ Reduce carga en BD y CPU
- ✅ Aceptable en contexto médico doméstico

---

## 🔍 Próximas extensiones arquitectónicas

### 1. **Strategy Pattern para notificaciones**
```java
interface NotificationStrategy {
    void notify(Recordatorio r);
}

class BeepStrategy implements NotificationStrategy {...}
class PopupStrategy implements NotificationStrategy {...}
class EmailStrategy implements NotificationStrategy {...}
```

### 2. **Factory Pattern para recordatorios**
```java
class RecordatorioFactory {
    static Recordatorio crear(String tipo) {
        return switch(tipo) {
            case "MEDICACION" -> new RecordatorioMedicacion();
            case "ACTIVIDAD" -> new RecordatorioActividad();
            ...
        };
    }
}
```

### 3. **Command Pattern para acciones del usuario**
```java
interface Command {
    void execute();
    void undo();
}

class MarcarHechoCommand implements Command {...}
class AplazarCommand implements Command {...}
```

---

## 📈 Escalabilidad futura

### Hacia microservicios:
```
┌─────────────────┐      ┌─────────────────┐
│  Frontend App   │◄────►│  API Gateway    │
└─────────────────┘      └────────┬────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              │                   │                   │
              ▼                   ▼                   ▼
      ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
      │ Recordatorios│   │ Notificaciones│   │  Adherencia  │
      │   Service    │   │   Service     │   │   Service    │
      └──────────────┘   └──────────────┘   └──────────────┘
              │                   │                   │
              └───────────────────┼───────────────────┘
                                  ▼
                          ┌──────────────┐
                          │  PostgreSQL  │
                          └──────────────┘
```

### Hacia Cloud:
- Scheduler → AWS Lambda + CloudWatch Events
- BD → RDS (PostgreSQL)
- Notificaciones → SNS/SES
- Storage → S3 para reportes

